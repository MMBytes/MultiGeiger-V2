# ============================================================================
# SHARED defaults — applied to every board build.
# Per-board overlays live in sdkconfig.defaults.<board>; CMake selects which
# one to chain after this file based on the BOARD variable.
# ============================================================================

# Custom partition table (factory + dual OTA, 2 MB each). Both boards use
# this layout; if a board needs different OTA slot sizes, override
# CONFIG_PARTITION_TABLE_CUSTOM_FILENAME in its overlay.
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions.csv"
# CONFIG_PARTITION_TABLE_SINGLE_APP is not set

# Note: CONFIG_LWIP_DHCP_SET_BROADCAST_FLAG existed in IDF 5.x; removed in 6.0.
# lwIP's dhcp.c now hard-codes "no broadcast flag" (comment: "we don't need the
# broadcast flag since we can receive unicast traffic before being fully
# configured"). On our AP this is fine — spike measured 1.1 s DHCP without it.

# Keep CONFIG_LWIP_DHCP_RESTORE_LAST_IP default (y) — REBOOTING fast path on 2nd+ boot.

# V2.6.15: lwIP SNTP client stock default is 1 server slot, silently capping
# every board (confirmed =1 on sparkfun_thing_plus_esp32c5, feathers3_d,
# heltec_v2 — never overridden anywhere in this repo). ntp.c's ntp_setup()
# calls esp_sntp_setservername(0..2, ...) to register all 3 configured NTP
# servers, but lwIP bounds-checks that index against SNTP_MAX_SERVERS and
# silently drops any call with idx >= it — so servers #2/#3 were configured
# in name only and never actually reachable. Worse, with MAX_SERVERS==1,
# lwIP's sntp.c compiles sntp_try_next_server() as a #define alias for
# sntp_retry() (components/lwip/lwip/src/apps/sntp/sntp.c) — the entire
# multi-server failover state machine doesn't exist in the binary, so a dead
# server 1 is retried forever (exponential backoff, capped ~1h) instead of
# ever falling through to servers 2/3. Root-caused when 10.11.12.150's
# chrony.service died and a C5 bench unit never fell back to its two public
# NTP servers despite them being configured and reachable. Bumped to 3 to
# match the 3 server slots ntp.c actually tries to fill.
CONFIG_LWIP_SNTP_MAX_SERVERS=3

# DHCP debug off for overnight soak (was filling log at ~10 KB/s). The lwIP
# master debug switch below being off already forces every lwIP sub-debug
# (DHCP included) off, so we don't list the children — an explicit child
# assignment is redundant and kconfig flags it as a "disabled symbol with a
# user-set value".
# CONFIG_LWIP_DEBUG is not set

# Log level: INFO, compile-time ceiling = INFO too. The ceiling was DEBUG
# (runtime-bumpable tags without a rebuild) until V2.6.21: with DEBUG compiled
# in, IDF's esp_vfs_select() has a live NULL-deref in its NULL-slot guard
# (vfs_calls.c:660 ESP_LOGD passes vfs->offset with vfs==NULL) — deterministic
# LoadProhibited (excvaddr 0xc) on the httpd task after any VFS unregister
# leaves a hole in s_vfs[] (SD card unmount after card pull; the hole persists
# until a mount succeeds again, and esp_vfs_unregister never shrinks
# s_vfs_count). INFO ceiling dead-code-eliminates that line project-wide.
# Bench-crashed twice 2026-07-16 before this fix. Don't raise back to DEBUG
# until the upstream ESP_LOGD is fixed.
CONFIG_LOG_DEFAULT_LEVEL_INFO=y
CONFIG_LOG_MAXIMUM_EQUALS_DEFAULT=y

# Wall-clock timestamp prefix on every log line (YY-MM-DD HH:MM:SS.sss).
# Pre-NTP: reads from RTC (starts at epoch 1970). Post-NTP: real Sydney local time.
# SYSTEM_FULL depends on LOG_VERSION_2 — default is V1 (ms since boot).
CONFIG_LOG_VERSION_2=y
CONFIG_LOG_TIMESTAMP_SOURCE_SYSTEM_FULL=y
# CONFIG_LOG_TIMESTAMP_SOURCE_RTOS is not set

# Bigger TCPIP task stack — we flagged 2560 as too small
CONFIG_LWIP_TCPIP_TASK_STACK_SIZE=4096

# V2.3.22: bump main task stack 8192 -> 16384. FTPS upload runs on the
# main task (called from log_ftp_loop in wifi_loop), and the TLS 1.3
# handshake + bidirectional close path is meaningfully deeper than the
# TLS 1.2 path it replaced. Diagnosed during pre1/pre2/pre3 — main task
# rebooted at FTPS time on both Heltec and FeatherS3-D before the bump.
# Original V2.0.24 history: bumped 3584 -> 8192 to fix display + reconnect
# storm overflow; that fix stays as the floor for backwards compatibility.
CONFIG_ESP_MAIN_TASK_STACK_SIZE=16384

# TX worker blocks on TLS for seconds at a time; that legitimately starves IDLE
# on its CPU. Stop the task watchdog from monitoring both IDLE tasks — the wdt
# is still armed for every other named task (main, tx, tcpip, wifi), so a real
# hang in one of those still fires.
# CONFIG_ESP_TASK_WDT_CHECK_IDLE_TASK_CPU0 is not set
# CONFIG_ESP_TASK_WDT_CHECK_IDLE_TASK_CPU1 is not set

# Enable gratuitous ARP after GOT_IP (default is on anyway, but be explicit)
CONFIG_LWIP_ESP_GRATUITOUS_ARP=y

# Mozilla CA bundle baked into firmware flash for esp_crt_bundle_attach() (M6 HTTPS TX).
# Parent symbol is default=y in IDF 6.0; full-bundle sub-choice is the default when on.
# (IDF 5.x: CONFIG_MBEDTLS_DEFAULT_CERTIFICATE_BUNDLE_FULL → IDF 6.0: _BUNDLE_DEFAULT_FULL.)
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=y

# V2.5.20 (review R1): allow esp-tls connections that deliberately carry NO
# verification option to proceed UNVERIFIED instead of failing the handshake.
# Required by MQTT TLS "Mode D — skip server verification" (and the Mode B
# empty-CA fallback) in mqtt.c: esp-mqtt's skip_cert_common_name_check only
# skips the CN match — without these two symbols a no-CA config aborts with
# "No server verification option set", so Mode D could never connect.
# Connections that DO attach the CA bundle / a custom CA still verify fully;
# this only changes the no-CA case from hard-fail to encrypted-but-unverified
# (which is exactly what the /config UI advertises for Mode D, LAN-only).
# NOTE: the committed per-board sdkconfig.<board> caches were updated in the
# same change — defaults only fill keys MISSING from an existing cache.
CONFIG_ESP_TLS_INSECURE=y
CONFIG_ESP_TLS_SKIP_SERVER_CERT_VERIFY=y

# TLS 1.3 alongside TLS 1.2 (IDF 6.0 default is 1.2 only). mbedTLS 4.x in
# this IDF supports 1.3 production-ready; we keep 1.2 enabled as fallback for
# any server that doesn't speak 1.3 yet. 1.3 saves 1 round trip per fresh
# handshake (1-RTT vs 2-RTT) and uses ephemeral key exchange by default.
# Cost: ~30–50 KB flash; partition is at ~46 % free so plenty of headroom.
CONFIG_MBEDTLS_SSL_PROTO_TLS1_3=y

# Enable client-side TLS session ticket *protocol support* in mbedTLS. Note:
# this alone does NOT auto-cache tickets across esp_http_client_init() calls
# — that requires manual mbedtls_ssl_get_session/_set_session plumbing.
# Setting the flag costs almost nothing and unlocks the option for later.
CONFIG_ESP_TLS_CLIENT_SESSION_TICKETS=y

# Build mbedTLS with -O2 instead of -Os. Costs ~30–100 KB flash; in return
# the software ECC point multiplication (the dominant cost in our ECDHE-RSA
# handshakes — neither ESP32 nor ESP32-S3 has dedicated ECC hardware) runs
# 10–20 % faster. The FeatherS3-D's larger I-cache amortises the bigger
# code more cleanly than the Heltec's smaller cache.
# V2.6.10: the ESP32-C5 (esp32c5/register/soc/ecc_mult_reg.h) DOES have a
# hardware ECC peripheral, and mbedTLS's MBEDTLS_HARDWARE_ECC defaults to y
# whenever SOC_ECC_SUPPORTED is set — so on that board mbedTLS uses hardware
# ECC point multiplication by default and this flag's "dominant cost"
# rationale doesn't apply the same way. Left enabled anyway (harmless, and
# still helps the non-ECC parts of the handshake), but don't assume this
# comment's justification is uniform across all 11 boards.
CONFIG_MBEDTLS_COMPILER_OPTIMIZATION_PERF=y
# CONFIG_MBEDTLS_COMPILER_OPTIMIZATION_SIZE is not set

# PSA crypto key-slot pool (V2.3.15). mbedTLS 4.x routes legacy crypto
# (mbedtls_aes_*, mbedtls_md_*, ECDHE key derivation, AES-GCM init) through
# PSA. Each TLS handshake allocates ~5–10 PSA slots transiently; AES-GCM
# encryption holds another ~2 per active connection. Our per-cycle load is
# 5 HTTPS targets (Madavi / SC / Radmon / OSM / aqi.eco) × ~5 slots each
# + FTPS ctrl + FTPS data ≈ 30+ active slot ops. Default 32 slots sits
# right at the edge; under transient peaks (concurrent FTPS + HTTPS, slow
# release between handshakes), exhaustion shows up as
# PSA_ERROR_INSUFFICIENT_MEMORY (-141 = -0x008D), which leaks out of
# mbedtls_ssl_setup / mbedtls_ssl_handshake as a bare error code that
# mbedtls_strerror cannot decode (PSA codes are a separate namespace).
# Bump to 128: cost ≈ +9 KB DRAM (96 extra slots × ~88 B each), gives 4×
# headroom against future feature growth (BMP581, DNMS, more upload targets).
#
# V2.4.3: the `CONFIG_MBEDTLS_PSA_KEY_SLOT_COUNT` kconfig symbol was
# REMOVED in IDF v6.0 / mbedtls 4.x (PSA config split out into
# psa/crypto_config.h). The setting still has to happen, just via a
# compile definition. See top-level CMakeLists.txt for the
# `idf_build_set_property(COMPILE_DEFINITIONS ...)` injection.

# OTA stability tweaks (lesson from V1.18.60): yield to TCPIP task during flash
# erase so the browser's upload XHR doesn't stall and drop with status 0.
CONFIG_SPI_FLASH_YIELD_DURING_ERASE=y
CONFIG_LWIP_TCP_WND_DEFAULT=16384

# V2.3.15: bump TCP send buffer from default 5744 (4*MSS, MSS=1460) to 16384
# (~11*MSS). The default fills after ~5 KB of unacknowledged data on the
# data socket during FTPS body upload — observed exactly at 5120 bytes
# (5 × FTP_WRITE_CHUNK at the V2.3.15-pre 1024 setting). When WiFi is
# transiently slow draining ACKs at that moment, our 15 s write deadline
# fires and we abandon the upload mid-stream. The abandoned-write tear-
# down then leaks PSA crypto state in mbedTLS 4.x (mbedtls_ssl_free from a
# WANT_WRITE-then-abandon path doesn't fully release the in-flight AES-GCM
# key handle), so subsequent retries fail with PSA_ERROR_INSUFFICIENT_MEMORY
# at the data-channel handshake. A bigger send buffer ≈ lower stall
# probability ≈ fewer leaks ≈ stable uploads even on near-full 60 KB rings.
# Cost: ~10 KB extra DRAM per active TCP socket (we have one FTPS upload
# at a time, so this is bounded).
CONFIG_LWIP_TCP_SND_BUF_DEFAULT=16384

# Bigger sys_evt stack (default 2304). Headroom for occasional verbose driver
# logs through the applog vprintf hook (1 KB line buffer) plus deeper event
# call chains. Costs ~4 KB DRAM.
CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=6144

# mbedTLS protocol debug intentionally disabled. Saves ~30 KB flash and
# avoids the high-volume callback chain. If diagnosis ever needs it back:
# add CONFIG_MBEDTLS_DEBUG=y here, mbedtls_ssl_conf_dbg + threshold(3) in
# log_ftp.c::tls_ctx_init, and bump main task stack accordingly.
# CONFIG_MBEDTLS_DEBUG is not set

# NOTE on CONFIG_MBEDTLS_DYNAMIC_BUFFER: enabled in V2.4.5 on the Heltec
# overlays ONLY, REVERTED in V2.4.7 (FTPS-reconnect TLSF heap corruption).
# V2.5.30 RE-TESTED on heltec_v2 against the ESP-IDF 6.0.1 mbedTLS dynamic-buffer
# fix — BOTH variants (DYNAMIC_BUFFER alone, then all three with FREE_CONFIG_DATA
# + FREE_CA_CERT) STILL CRASHED (same TLSF heap-corruption panic on the first
# FTPS upload, free site wandering across 3 runs = heap corruption upstream of
# session_reset). REVERTED again, stays OFF on all overlays — it's our hand-rolled
# log_ftp.c teardown, not IDF; don't re-test the kconfig on future bumps. See the
# detailed post-mortem in sdkconfig.defaults.heltec_v2. The FeatherS3-D and QT Py have abundant
# PSRAM heap so the per-record alloc/free CPU overhead isn't worth paying there;
# only the Heltec's tight internal-DRAM budget would have benefited.

# --- PSRAM heap policy (V2.5.33) — applies to PSRAM boards only --------------
# These four knobs are byte-identical across every PSRAM board (FeatherS3-D,
# XIAO S3, QT Py PICO, heltec_wifi_lora32_v4_r2, sparkfun_thing_plus_esp32s3,
# sparkfun_thing_plus_esp32c5, adafruit_esp32s3_tft_feather,
# adafruit_esp32_feather_v2), so they live
# here once instead of being copy-pasted into each per-board overlay (drift
# risk). All four are kconfig-gated on CONFIG_SPIRAM (set per-board), so on
# the Heltec V2 boards
# (no PSRAM) they are invisible and simply ignored — only the SPIRAM
# mode/type/speed lines, which genuinely differ per board, stay in the
# overlays.
#
# Root-cause fix for the INTERNAL-DRAM fragmentation that armed the heap-guard
# auto-reboot (esp32-5965048: INTERNAL largest-free 68K->39K when an inbound
# /config plus per-cycle outbound TLS POSTs churn small buffers in the tiny
# internal arena). Routing the network/TLS buffers to PSRAM keeps the internal
# arena defragmented:
#   * WiFi + lwIP pbufs/PCBs -> PSRAM (the bisecting allocator we traced)
#   * mbedTLS record/session -> PSRAM (the big per-cycle internal churn)
# ALWAYSINTERNAL lowered 16384 -> 4096 so medium app buffers spill too. DMA- and
# IRAM-required allocations still pass MALLOC_CAP_DMA/INTERNAL and stay internal,
# and RESERVE_INTERNAL=40960 guarantees internal headroom for them.
CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=4096
CONFIG_SPIRAM_MALLOC_RESERVE_INTERNAL=40960
CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP=y
CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y

# V2.4.18: enable ESP-IDF core dump to flash for over-the-air panic
# recovery. Pairs with the 64 KB `coredump` partition row added to
# partitions.csv and partitions_4mb.csv, and with the new
# main/coredump.c module + GET /coredump.elf endpoint. On panic, IDF
# writes the panicking task's register state + every other task's
# stack snapshot into the partition before reboot. After reboot,
# coredump_init() probes the partition, parses the summary, and
# surfaces "task=<NAME> PC=0x...." on the status page. Operator
# downloads the raw .elf, decodes off-device with
# `espcoredump.py info_corefile -t elf -c dump.elf build_<board>/geiger_v2.elf`.
# ELF format (BIN is legacy, harder to decode). CRC32 checksum (SHA256
# is for secure-boot setups we don't use).
CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH=y
# CONFIG_ESP_COREDUMP_ENABLE_TO_UART is not set
# CONFIG_ESP_COREDUMP_ENABLE_TO_NONE is not set
CONFIG_ESP_COREDUMP_ENABLE=y
CONFIG_ESP_COREDUMP_LOGS=y
# V6.0 migration note: CONFIG_ESP_COREDUMP_DATA_FORMAT_ELF / _BIN and
# CONFIG_ESP_COREDUMP_CHECKSUM_CRC32 / _SHA256 were removed in IDF v6.0.
# ELF format + CRC32 checksum are now the implicit (and only) options.
# Setting the old symbols printed "unknown kconfig symbol" warnings on
# every build; removing them is the right cleanup.
CONFIG_ESP_COREDUMP_CHECK_BOOT=y
CONFIG_ESP_COREDUMP_MAX_TASKS_NUM=64
